#pragma once

// Fail-fast existence checks for demo shaders/models/textures (after asset root is configured).
namespace UtilStartupChecks {
void VerifyRequiredAssets();
}  // namespace UtilStartupChecks

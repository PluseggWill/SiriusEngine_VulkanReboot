---
description: VulkanDesktop 目录/命名/VS 过滤器 + C++/Vulkan 实现要点
globs: VulkanDesktop/**/*
alwaysApply: false
---

# VulkanDesktop — 目录与命名

## 模块划分（+ 入口）

| 模块 | 磁盘目录 | 职责 |
|------|----------|------|
| **App** | `VulkanDesktop/App/` | Application lifecycle, input, **WorldState (target)**, debug overlay |
| **Util** | `VulkanDesktop/Util/` | 日志、文件 IO、帧统计、ImGui **widgets/panels**（Vulkan ImGui 后端在 RenderCore `Vk_ImGuiLayer`） |
| **Platform** | `VulkanDesktop/Platform/` | `Pf_PlatformHost` + GLFW concrete host (window/surface/frame timing) |
| **RenderContract** | `VulkanDesktop/RenderContract/` | `Gpu_*.h` UBO/shared structs only — no `App`/`Gfx`/`RenderCore`/`Util` includes |
| **RenderCore** | `VulkanDesktop/RenderCore/` | Vulkan 设备、交换链、管线、录制 |
| **Gfx** | `VulkanDesktop/Gfx/` | 场景/视图数据（mesh、material、fly camera），与 API 无关 |
| **入口** | `VulkanDesktop/VulkanDesktop.cpp` | `main`，留在项目根 |

着色器：源 `VulkanDesktop/Shader/`，生成物 `Shader_Generated/`（`.spv`）。VS 过滤器 **Gfx\Shader**、**Gfx\Shader Generated**。详见 **`shader-build.mdc`**。

## Visual Studio（`.vcxproj.filters`）

与磁盘对齐，不用默认 `Source Files` / `Header Files`。新增/移动源文件时**同时**更新 `.vcxproj` 与 `.vcxproj.filters`。

**常见坑：** 只改磁盘、漏加 vcxproj → 调用方编译通过但链接 **LNK2019**（未定义 `Util*` / `Vk*` 符号）。加完文件后跑 `Verify-CI.ps1` 或全量 Rebuild 确认链接通过。

```
VulkanDesktop.cpp
App/           ← Application, InputSystem, WorldState (peel target)
Util/          ← Util_* ；ThirdParty/imgui/
Platform/      ← Pf_PlatformHost, Pf_GlfwPlatformHost
RenderContract/← Gpu_*.h
RenderCore/    ← Vk_*
Gfx/
  Shader/              ← *.vert, *.frag, CompileShader*.bat
  Shader Generated/    ← *.spv（勿手改）
```

## 命名

- 文件：`{Util|Vk|Gfx|Pf}_{Name}.h/.cpp`；GPU UBO headers → `Gpu_*.h` under `RenderContract/`
- 类型：与文件名一致（`Vk_Renderer`, `Gfx_Mesh`, `Pf_PlatformHost`, `Gpu_CameraData`）；描述符成员 `eVk_*`
- Util 自由函数 → `UtilLogger::` 等；RenderCore 初始化辅助 → `VkInit::`
- 成员 `my*`，参数 `a*`

### 类型归属

- **Util**：Logger、Loader（文件 IO）、ImGui 面板 widgets、统计
- **Util 面板：** 一类调试 UI 一个 `Util_*Panel.{h,cpp}`；在 `App/DebugOverlay.cpp` 的 Engine Debug 标签页挂载。面板编辑 settings DTO / Actions，不 `#include Vk_Renderer`；副作用由 App 施加。面板内只做 `BuildContents`，不负责 `ImGui::Begin/End`。Vulkan ImGui render-pass/FB → `Vk_ImGuiLayer`。
- **Pf_**：platform host seam（window/surface/timing）
- **Vk_**：设备、交换链、管线、录制、RAII 缓冲
- **Gfx_**：mesh/material/场景 SoA / `Gfx_RenderCamera`；新类型优先 `Gfx/`，勿塞进 `Vk_Types.h`
- **Gpu_***：shader contract structs in `RenderContract/` only

### Include

- 同目录：`#include "Vk_Core.h"`
- 跨模块：`#include "../Util/Util_Logger.h"`
- 入口：`#include "RenderCore/Vk_Core.h"`

### 反例

- 无前缀类型名；把 fly camera 放回 `RenderCore`；`Gpu*` 契约塞进 `Vk_*` 头；改 Vulkan 结构体字段名（保持 `pQueueFamilyIndices`）

## C++ / Vulkan 实现

- **热路径零分配**：录制/每帧更新中禁止隐式堆分配（未 `reserve` 的 `push_back`、`string` 拼接、`new`）
- **所有权**：`unique_ptr`/`shared_ptr`；裸指针仅非拥有观察
- **RAII**：`vkCreate*` 对称销毁或 deletion queue；GPU struct 用 `alignas` + std140/std430
- **安全**：枚举 `switch` 全覆盖或显式 `default`；`VkResult` 等 `[[nodiscard]]`
- **错误**：Debug 用 `VK_CHECK` 等，不吞 API 失败
- **Bindless vs batch:** follow [`shader-bindless-policy_Plan.md`](../../Docs/Archived/plans/shader-bindless-policy_Plan.md) §Maintenance contract — not “always bindless”; batch fallback required
- **格式化**：根目录 `.clang-format`；本地 `pwsh -File Scripts/Assert-ClangFormat.ps1 -Fix [-BootstrapTools]`；G0 在 `Verify-CI.ps1` 中 dry-run

注释与 shader/CPU 契约见 **`cpp-comments.mdc`**。Per-draw 描述符见 **`vulkan-descriptor-per-draw.mdc`**。RenderCore pass 见 **`vulkan-render-pass-pitfalls.mdc`**。非 vibe 改动见 **`vulkan-desktop-quick.mdc`**。
